// import { createApp } from '@/app';
// import { Paths } from '@/config';
// import { getEnv } from '@/lib';
//
// createApp({
//   ensureDirs: [Paths.REPOSITORIES, Paths.UPLOAD],
//   port: getEnv('port'),
// }).start();

import { isArray } from 'shared';
